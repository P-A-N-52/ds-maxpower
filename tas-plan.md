# TaS 在 Pi 上的验证与实现方案

> 2026-09-06 修订：先用独立 API 实验验证输入、轨迹和结果，再接入 Pi 扩展。
> Pi 源码保持不变。独立评测器位于 `tas-lab/`；命令控制的首版扩展已在 `tas-extension/` 实现并完成真实 DeepSeek 工具循环验收。
> 原理依据：[Trace as State](https://arxiv.org/html/2609.02702v1)。

## 1. 目标与证据边界

同一任务独立生成 N 份推理轨迹，再让模型带着轨迹重新读取原文并作答。
首先验证在固定模型与任务上，轨迹放在原文之前是否比之后更有效；随后才判断
它是否值得用于真实编程任务。

论文证据来自特定长上下文任务。短任务、真实工具循环、压缩后的轨迹、
自适应 N，以及 2–3 遍是否最划算，都属于待测假设。

## 2. 输入合同

- `x`：冻结的原文或文件快照。
- `q`：单独的问题与答案格式要求。
- `T`：由同一批首遍推理文本序列化得到的轨迹块。

| 条件 | 输入 |
| --- | --- |
| First pass | `[x, q]` |
| Trace append | `[x, T, q]` |
| Trace as state | `[T, x, q]` |

问题始终最后；两种反馈条件共享字节相同的 x、q 和 T，只改变 T 的位置。
系统提示、模型、reasoning effort、输出上限及答案解析规则保持一致。
期望答案只供本地判分，不能进入模型请求。

命令中的 `@文件` 不等于已读取文件全文。Pi 扩展命令处理器接收原始参数；
扩展现已显式读取并冻结 x，不能依赖无工具的首遍自行访问路径。
已有会话消息也不能隐式充当 x：把 T 追加进会话，无法保证它排在旧消息之前。

## 3. 当前 API 评测器

实现目录：`tas-lab/`。使用 Kimi Code 现有 DeepSeek 配置，只读加载凭据，
不修改配置，不向日志、参数或结果文件写入密钥。

本次现场确认：Kimi Code 0.38.0；provider 为 `deepseek`、`type = "openai"`，
官方地址 `https://api.deepseek.com/v1`；选择 `deepseek-v4-pro`。
这些是本次快照，后续运行必须重新检查，不能根据旧笔记自动切换协议或模型。

- Chat Completions：显式 `thinking.type = "enabled"`、`reasoning_effort = "max"`。
- 流式收集 `reasoning_content`、最终文本、finish reason 和 usage。
- 只有完整终止且包含非空 reasoning 的首遍才进入 T；不按答案正确性筛选。
- 失败、超时和截断保留原始记录，不静默重试；本轮遇到不完整首遍即停止扩展样本。
- 单次请求无历史和工具，不受 Pi 会话压缩影响。

固定实验为四个本地生成图题：入边检索和最短路径距离检索，各含两个上下文长度。
上下文约 120k / 300k **字符**，实际 token 数以服务端 usage 为准。
每题 2 次首遍、1 次 append、1 次 state，加独立冒烟共最多 17 次请求。
生成器与独立图算法验证标准答案；不根据模型表现换题。

2026-09-06 首轮长图 pilot 的两个首遍均在 16,384 输出 token 截断，尚未产生答案，
因此未进入 append/state。该批保留为容量诊断。另设独立校准批次：24k/60k 字符、
新固定种子、每次 32,768 输出 token、最多 8 次请求；两批结果不能混成一次复现。

完整命令、预算、输出目录和解释限制见 [tas-lab/README.md](tas-lab/README.md)。
该实验不是 GraphWalks 官方数据集复现，也不是 Pi 扩展端到端测试。

## 4. 轨迹长度与全局预算

用户指定的目标扩展参数仍为每条最多 **50,000 token**。
论文实际使用每条最多 **50,000 字符**；当前 API pilot 使用论文的字符单位，
二者不能混用或都称为相同复现配置。

Pi 的 `estimateTextTokens()` 采用字符数除以 4 的启发式，不能提供严格的
50k-token 上限保证；固定中文修正倍数也不是 tokenizer 保证。
实现 token 上限前，应选定与实际模型匹配的 tokenizer 或可信 token 计数接口，
验证中英文、代码、数字与特殊字符边界。没有该能力时，只能明确提供字符上限模式。

总预算覆盖：系统提示 + 历史（若有）+ T + x + q + 工具定义/结果 + 输出预留。
先计算可分配给 T 的空间，再分配每条限额；不能仅依据 N × 单条上限判断窗口足够。
预算不足应在发送前明确失败或要求调整参数，不静默删除原文或换模型。

## 5. 已实现的 Pi 命令模式

入口为 `tas-extension/index.ts`，协议和预算在 `protocol.ts`，归档在 `archive.ts`。
使用 `/tas on` 开启、`/tas off` 关闭并取消、`/tas status` 查看；默认关闭，当前会话有效。
启动与参数说明见 [tas-extension/README.md](tas-extension/README.md)。

首遍放在 Pi 原生 `context` 生命周期内，通过 `ModelRegistry.complete()` 顺序调用两次，
读取相同的冻结上下文和显式 `@文件` 快照，不提供工具。均使用 max reasoning、
32,768 输出 token 上限、10 分钟超时，不重试首遍。仅完整非空 thinking 才进入 T。

最终阶段接入正常 Pi 工具循环，以请求级上下文重建保证 T 在原文与历史前；
不使用 `sendUserMessage()` 追加轨迹，不污染持久会话。工具续轮复用同一份 T/x，
新的用户任务重新采样。首版每条轨迹使用 50,000 Unicode 字符上限，严格 token 裁剪尚未实现。
预算采用保守字节估算并预留输出空间，不把估算宣称为精确 tokenizer。

显式连接 Pi 原生取消信号；扩展错误先 abort，避免 Pi 吞掉异常后退回普通请求。
需要继续同一任务时禁止压缩替换输入，任务完成后的压缩正常进行。
会话切换和模型切换取消活跃任务，`agent_settled` 结算最终状态，迟到用量保留且不回退终态。

2026-09-06 已完成 27 项离线测试、源码终端命令验收和真实 DeepSeek 验收：
两次首遍、一次真实 read、一次最终回答共 4 次请求，检查实际 max 参数、相同输入、
轨迹前置与工具续轮复用。这证明接入可用，不代表真实编程效果已被验证。

## 6. 方案 B：带工具的独立首试

Pi subagent 示例提供进程与 JSON 事件编排，但默认共享 cwd。
进程独立不等于工作区独立；几个首遍同时写同一仓库会破坏输入一致性。

先选择一种明确边界：

- 只读探索：禁用写入和任意命令执行能力，所有首遍基于同一不可变文件快照。
- 可写试做：每遍从同一快照创建隔离工作目录；外部服务等共享副作用另行限制。

以最终 `message_end` 为消息事实，delta 只作进度；验证整个任务的终止状态，
不能仅把 JSON 子进程的 exit code 当成模型成功。明确超时、进程清理、异常退出、
部分轨迹持久化与最终 pass 的启动门槛。

原始事件完整归档。论文模式可以只重放 thinking；工程模式需另测保留文件片段、
测试结果等关键观察的版本，不能先假设所有 toolResult 都是噪音。

## 7. 评测与后续里程碑

1. **API 冒烟与 pilot**：确认流式 reasoning、完整终止、配对输入、独立 oracle、
   usage 和失败留档。当前已实现评测器，结果以各次运行的 manifest/report 为准。
2. **补足方法证据**：扩大固定样本量和反馈重复数，加入相近实际预算的直接多采样
   对照，报告成功率、置信区间、输入/缓存/输出 token、耗时与失败原因。
3. **Pi 方案 A**：首版模式已接入，离线生命周期测试及真实模型工具循环通过；
   后续补精确 token 裁剪、更多模型/扩展组合和真实编程任务评测。
4. **方案 B**：在隔离与事件合同完善后，才开展真实编程任务。

首遍 N 次加最终一遍描述的是调用次数，不等于严格的费用倍数。
两种反馈共用首遍时，实验总账和单策略部署账需要分开；缓存命中由服务端管理，
重复首遍可以复用原文前缀，不能写成“各遍 prompt 不同所以缓存无收益”。
usage 缺失时标明费用只是已知部分，不能当作完整账单。

文献线索中的“无人研究”“最佳 N”“短任务收益消失”等判断，在没有充分检索和
直接实验之前均保留为假设，不作为项目收益承诺。
